name = "nomoney"
compatibility_date = "2024-09-25"
pages_build_output_dir = "dist"

# KV stores both the page JSON (key = slug) and the share PNG (key = "img:"+slug).
# Create the namespace, then paste its id below (see DEPLOY.md).
[[kv_namespaces]]
binding = "PAGES"
id = "3c15cd86ba634f76aa3e0f685be49d77"

# Workers AI — powers the "rewrite my sob story (AI)" button (functions/api/ai.js)
[ai]
binding = "AI"
