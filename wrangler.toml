name = "nomoney"
compatibility_date = "2024-09-25"
pages_build_output_dir = "."

# KV stores both the page JSON (key = slug) and the share PNG (key = "img:"+slug).
# Create the namespace, then paste its id below (see DEPLOY.md).
[[kv_namespaces]]
binding = "PAGES"
id = "REPLACE_WITH_KV_NAMESPACE_ID"
